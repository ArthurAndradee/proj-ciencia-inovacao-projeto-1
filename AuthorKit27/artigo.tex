%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%  Alocação de computação de inferência na síntese de programas — ARC-AGI-1
%  Template: AAAI Press AuthorKit 2027 (aaai2027.sty)
%
%  ============================================================================
%  COMO ATUALIZAR ESTE ARTIGO QUANDO OS RESULTADOS MUDAREM
%  ============================================================================
%  Todo número do texto, das tabelas e das figuras vem de um \newcommand no
%  PAINEL DE RESULTADOS abaixo. Nada é digitado solto no corpo.
%
%   1. Troque os valores no PAINEL. Macros terminadas em "N" são a versão
%      numérica (ponto decimal) usada pelas figuras; a irmã, sem "N", é a de
%      exibição (vírgula decimal). Vêm em pares na mesma linha — troque as duas.
%      Valores com sinal usam \mbox{$-$14{,}8}, que compila em texto e em
%      modo matemático. Mantenha esse formato.
%   2. Troque \vereditoUm, \vereditoDois e \vereditoTres, logo abaixo do painel.
%   3. Reescreva apenas os quatro parágrafos marcados com % >>> VEREDITO.
%      O resto — motivação, trabalho relacionado, metodologia, limitações — é
%      independente do sinal do resultado. O título também é.
%
%  ALVO DE EXTENSÃO: 6 páginas. Antes de acrescentar qualquer parágrafo, figura
%  ou tabela, compile e confira com AuthorKit27/compilar.sh.
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\documentclass[letterpaper]{article} % DO NOT CHANGE THIS
\usepackage[submission]{aaai2027}  % DO NOT CHANGE THIS
\usepackage[hyphens]{url}  % DO NOT CHANGE THIS
\usepackage{graphicx} % DO NOT CHANGE THIS
\urlstyle{rm} % DO NOT CHANGE THIS
\def\UrlFont{\rm}  % DO NOT CHANGE THIS
\usepackage{natbib}  % DO NOT CHANGE THIS AND DO NOT ADD ANY OPTIONS TO IT
\usepackage{caption} % DO NOT CHANGE THIS AND DO NOT ADD ANY OPTIONS TO IT
\frenchspacing  % DO NOT CHANGE THIS

\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{amsfonts} % \mathbb na Equação 1
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,shapes.geometric,calc,fit}

\pdfinfo{
/TemplateVersion (2027.1)
}

% O AuthorKit proíbe babel. Como o artigo está em português, os nomes dos
% flutuantes são redefinidos à mão — não altera layout, fonte nem espaçamento.
\renewcommand{\figurename}{Figura}
\renewcommand{\tablename}{Tabela}
\renewcommand{\abstractname}{Resumo}
\renewcommand{\refname}{Referências}

% O aaai2027.sty escreve "Abstract" literalmente e ignora \abstractname. O bloco
% abaixo é cópia exata da definição do estilo (l. 208-218) trocando só a palavra;
% nenhuma métrica muda. APAGUE se for submeter à AAAI.
\renewenvironment{abstract}{%
  \centerline{\bf \abstractname}%
  \vspace{0.5ex}%
  \setlength{\leftmargini}{10pt}%
  \begin{quote}%
    \small%
}{%
  \par%
  \end{quote}%
  \vskip 1ex%
}%

\setcounter{secnumdepth}{2}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%  PAINEL DE RESULTADOS
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\newcommand{\modelo}{\texttt{gemini-3.5-flash-lite}}
\newcommand{\orcamento}{7}
\newcommand{\semente}{20260814}
\newcommand{\divisao}{\texttt{evaluation}}
\newcommand{\alfaBonf}{0{,}01}
\newcommand{\numComparacoes}{5}

% ---- Medição principal: 270 tarefas, 4 condições ----------------------------
\newcommand{\An}{270}
\newcommand{\AsampRes}{88} \newcommand{\AsampAcc}{32{,}6}
\newcommand{\AcritRes}{93} \newcommand{\AcritAcc}{34{,}4}
\newcommand{\AnoorRes}{80} \newcommand{\AnoorAcc}{29{,}6}
\newcommand{\AcegisRes}{83}\newcommand{\AcegisAcc}{30{,}7}
\newcommand{\AsampTreino}{35{,}6} \newcommand{\AcritTreino}{35{,}6}
\newcommand{\AnoorTreino}{33{,}3} \newcommand{\AcegisTreino}{34{,}8}
\newcommand{\AsampProg}{5{,}43} \newcommand{\AcritProg}{3{,}23}
\newcommand{\AnoorProg}{3{,}27} \newcommand{\AcegisProg}{3{,}34}
\newcommand{\AsampTok}{28{,}0} \newcommand{\AcritTok}{48{,}1}
\newcommand{\AnoorTok}{45{,}8} \newcommand{\AcegisTok}{48{,}8}
\newcommand{\AtokExcessoMin}{63} \newcommand{\AtokExcessoMax}{74}
\newcommand{\AreducaoProg}{40}
\newcommand{\AdeltaCritSamp}{\mbox{$+$1{,}9}}
\newcommand{\Aleaks}{0} \newcommand{\Acriticas}{1.844}
\newcommand{\CegisConf}{630/630}

% ---- As 5 comparações pré-registradas ---------------------------------------
% IC de Wilson pela Equação 2, validado contra a medição sob P1, cujo IC
% publicado em docs/results.md é reproduzido exatamente.
\newcommand{\CumDisc}{51}  \newcommand{\CumSplit}{$23{\times}28$} \newcommand{\Cump}{0{,}5758}
\newcommand{\CumLoN}{-3.3} \newcommand{\CumHiN}{6.7}
\newcommand{\CdoisDisc}{54}\newcommand{\CdoisSplit}{$31{\times}23$} \newcommand{\Cdoisp}{0{,}3409}
\newcommand{\CdoisLoN}{-7.9}\newcommand{\CdoisHiN}{2.3}
\newcommand{\CtresDisc}{47}\newcommand{\CtresSplit}{$26{\times}21$} \newcommand{\Ctresp}{0{,}5601}
\newcommand{\CtresLoN}{-6.5}\newcommand{\CtresHiN}{3.0}
\newcommand{\CquatroDisc}{47}\newcommand{\CquatroSplit}{$30{\times}17$}\newcommand{\Cquatrop}{0{,}0789}
\newcommand{\CquatroLo}{\mbox{$-$9{,}1}}\newcommand{\CquatroHi}{\mbox{$+$0{,}2}}
\newcommand{\CquatroLoN}{-9.1}\newcommand{\CquatroHiN}{0.2}
\newcommand{\CcincoDisc}{54}\newcommand{\CcincoSplit}{$32{\times}22$} \newcommand{\Ccincop}{0{,}2203}
\newcommand{\CcincoLoN}{-8.5}\newcommand{\CcincoHiN}{1.6}
\newcommand{\pMenor}{0{,}0789}

% ---- Desconto da primeira geração -------------------------------------------
\newcommand{\GsampTot}{88} \newcommand{\GsampUm}{45} \newcommand{\GsampCiclo}{43}
\newcommand{\GcritTot}{93} \newcommand{\GcritUm}{44} \newcommand{\GcritCiclo}{49}
\newcommand{\GnoorTot}{80} \newcommand{\GnoorUm}{35} \newcommand{\GnoorCiclo}{45}
\newcommand{\GcegisTot}{83}\newcommand{\GcegisUm}{32}\newcommand{\GcegisCiclo}{51}
\newcommand{\RpMenor}{0{,}2962} \newcommand{\RpMaior}{1{,}0000}
\newcommand{\SeedEconomia}{810}

% ---- Réplica sob o protocolo P1 (2 condições, mesmas 270 tarefas) -----------
\newcommand{\BsampRes}{88} \newcommand{\BcritRes}{86}
\newcommand{\BsampPrimeira}{44} \newcommand{\BcritPrimeira}{42}
\newcommand{\BsampCiclo}{44} \newcommand{\BcritCiclo}{44}
\newcommand{\BdiscrimN}{206}
\newcommand{\Bdelta}{\mbox{$-$0{,}7}} \newcommand{\Bp}{0{,}8877}
\newcommand{\BicBaixo}{\mbox{$-$5{,}6}} \newcommand{\BicAlto}{\mbox{$+$4{,}3}}
\newcommand{\BicAltoAbs}{4{,}3}

% ---- Estabilidade: subamostragem sem reposição das próprias 270 -------------
% Gerado por AuthorKit27/reamostragem.py sobre results/runs/critic-official/.
\newcommand{\SubRep}{20.000}
\newcommand{\SubNa}{30} \newcommand{\SubNb}{60}
\newcommand{\SubDirA}{51} \newcommand{\SubDirAN}{51.4}
\newcommand{\SubDirB}{58} \newcommand{\SubDirBN}{58.2}
\newcommand{\SubDirCN}{62.9} \newcommand{\SubDirDN}{71.1} \newcommand{\SubDirEN}{79.9}
\newcommand{\SubOrdA}{8}  \newcommand{\SubOrdAN}{7.7}
\newcommand{\SubOrdB}{12} \newcommand{\SubOrdBN}{11.6}
\newcommand{\SubOrdCN}{17.4} \newcommand{\SubOrdDN}{27.2} \newcommand{\SubOrdEN}{41.6}
\newcommand{\SubBandaAlo}{\mbox{$-$10{,}0}} \newcommand{\SubBandaAhi}{\mbox{$+$13{,}3}}
\newcommand{\ProjN}{91} \newcommand{\ProjPextrapolado}{0{,}0027}
\newcommand{\ProjPfinal}{0{,}0789}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%  VEREDITOS — as três afirmações interpretativas do artigo.
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\newcommand{\vereditoUm}{a revisão guiada por oráculo \textbf{não} superou a
amostragem independente}
\newcommand{\vereditoDois}{\textbf{nenhuma} das \numComparacoes{} comparações
pré-registradas é significativa — nem sob Bonferroni, nem a $\alpha = 0{,}05$
sem correção}
\newcommand{\vereditoTres}{cerca de metade das vitórias de qualquer condição é
decidida na primeira geração, que é idêntica entre elas}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%  Estilo local das figuras TikZ
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\tikzset{
  cx/.style={draw, rounded corners=2pt, align=center, font=\scriptsize,
             inner sep=3pt, minimum height=5.5mm},
  agente/.style={cx, fill=black!10, minimum width=19mm},
  proc/.style={cx, fill=white, minimum width=19mm},
  dec/.style={draw, align=center, font=\scriptsize, rounded corners=6pt,
              inner sep=3pt, fill=white},
  oraculo/.style={cx, dashed, fill=black!4},
  fl/.style={-{Latex[length=1.4mm]}, thin},
  flt/.style={-{Latex[length=1.4mm]}, thin, dashed},
  rot/.style={font=\scriptsize, inner sep=1.5pt, fill=white},
  cel/.style={draw, rounded corners=2pt, align=center, font=\scriptsize,
              text width=25mm, minimum height=12mm, inner sep=2pt},
  celcheia/.style={cel, fill=black!10},
  celvazia/.style={cel, dashed, text=black!55},
  eixo/.style={font=\scriptsize\bfseries, align=center, inner sep=1.5pt}
}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%  TÍTULO — neutro quanto ao resultado; não muda se o sinal inverter.
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\title{Alocação de Computação de Inferência na Síntese de Programas com
Modelos de Linguagem}

\author{Anonymous Submission}
\affiliations{}

\begin{document}
\maketitle

\begin{abstract}
Um orçamento fixo de chamadas a um modelo de linguagem admite duas alocações
incompatíveis: \emph{diversificar}, gerando $N$ candidatos independentes, ou
\emph{iterar}, revisando um único candidato com feedback. A literatura relata
ganhos para a segunda via e também falhas sistemáticas de autocorreção, e as
duas leituras são difíceis de separar porque, sem informação externa, o revisor
não sabe que errou. Removemos esse confundimento concedendo à via iterativa uma
vantagem que ela não teria em produção: um Agente Crítico que enxerga o gabarito
do par de teste e devolve apenas contradições em linguagem natural, sem propor
solução e sem participar da seleção do candidato final — um limite superior
otimista da via iterativa.
% >>> VEREDITO (resumo)
Em \An{} tarefas do ARC-AGI-1, sob orçamento igualado, \vereditoDois{}. O achado
mais transferível independe do sinal do efeito: \vereditoTres{}. Descontada essa
parcela, a medição discrimina em cerca de um quinto da amostra e a ordenação
entre as condições inverte em relação ao número bruto. Por subamostragem do
mesmo conjunto, em \SubNa{} tarefas a direção observada se reproduz em
\SubDirA\% das vezes e a ordenação completa das quatro condições em
\SubOrdA\% — um confundimento herdado por qualquer comparação entre esquemas de
inferência que partam do mesmo \emph{prompt}, e que nenhuma métrica agregada
revela.
\end{abstract}

\section{Introdução}

Uma fração substancial do desempenho recente em tarefas de raciocínio vem de
escalar a computação no momento da inferência, e não os parâmetros do modelo
\citep{snell:24,brown:24}. Isso converte uma questão antes implícita em decisão
de engenharia com custo direto: dado um orçamento fixo de chamadas, como
alocá-lo?

Há duas respostas estruturalmente distintas. \textbf{Diversificar} é gerar $N$
programas independentes e selecionar o melhor por um verificador barato — a
receita de \emph{pass@k} em geração de código \citep{chen:21} e da filtragem
massiva do AlphaCode \citep{li:22}. \textbf{Iterar} é gastar o mesmo orçamento
revisando um único candidato à luz de feedback, como em \emph{self-debugging}
\citep{chen:24}, \textsc{Self-Refine} \citep{madaan:23} e \textsc{Reflexion}
\citep{shinn:23}.

A evidência sobre a segunda via é contraditória. Os trabalhos originais relatam
ganhos expressivos; réplicas controladas encontram que grande parte deles
desaparece quando o orçamento é igualado, ou quando o modelo precisa detectar
sozinho que errou \citep{huang:24,stechly:23,olausson:24}. A explicação candidata
mais simples é que autocrítica não é crítica: sem informação externa sobre o
próprio erro, o modelo tende a reafirmar a resposta que já produziu. Sob essa
hipótese, o fator limitante não é a estratégia iterativa, mas a qualidade do
canal de feedback.

Testamos essa hipótese maximizando exatamente a variável que ela aponta. Nosso
Agente Crítico enxerga o gabarito do par de teste, que o Gerador nunca vê, e é
restrito por construção: não propõe solução, não escreve código e não reproduz
grades, limitando-se a apontar em que pontos a regra enunciada e o programa
candidato contradizem a realidade. Um filtro automático redige qualquer grade ou
código que escape em sua saída, e o gabarito nunca participa do critério de
parada nem da seleção do candidato final. O desenho é, portanto, deliberadamente
favorável à via iterativa: o crítico dispõe de informação privilegiada que nenhum
sistema autônomo teria e ainda assim consome o mesmo orçamento que a amostragem.
Ele funciona como \textbf{limite superior otimista}, o que torna a leitura
informativa nos dois sentidos — se nem ele vencer, a vantagem atribuída ao
feedback iterativo fica sob suspeita muito além deste arranjo.

Avaliamos no ARC-AGI-1 \citep{chollet:19}, em que cada tarefa traz alguns pares
entrada\,$\rightarrow$\,saída de demonstração e exige inferir a transformação.
O domínio é adequado porque a resposta é verificável exatamente, os pares de
demonstração fornecem um verificador barato que ambas as estratégias usam sem
tocar no gabarito, e o \emph{benchmark} permanece resistente.

\paragraph{Contribuições.} Um protocolo pareado com orçamento igualado e
salvaguardas auditáveis contra contaminação pelo oráculo; uma decomposição
fatorial do mecanismo em dois eixos — acesso ao oráculo e forma do feedback — em
\An{} tarefas, com \numComparacoes{} comparações pré-registradas sob correção de
Bonferroni; e a quantificação de quanto de uma comparação entre arquiteturas de
agentes é atribuível à arquitetura, com a medida correspondente de
reprodutibilidade da ordenação.

\section{Trabalho Relacionado}

Amostrar muitos candidatos e filtrar é a estratégia de referência em síntese de
programas desde o Codex \citep{chen:21}; \citet{li:22} levaram a ideia ao limite,
e trabalhos recentes caracterizam a curva de cobertura \citep{brown:24} e os
regimes em que essa alocação supera aumentar o modelo \citep{snell:24}. Nossa
condição \texttt{sampling} é uma
instância deliberadamente simples dessa família, com o verificador mais honesto
que o ARC oferece — os próprios pares de demonstração —, o que mantém a
comparação livre de qualquer componente treinado.

A via iterativa realimenta o modelo com o resultado da execução \citep{chen:24}
ou com crítica gerada por ele mesmo \citep{madaan:23,shinn:23}. A contestação foi
rápida: \citet{huang:24} argumentam que modelos não corrigem raciocínio sem sinal
externo, \citet{stechly:23} mostram que a melhora aparente provém do verificador
embutido no protocolo, e \citet{olausson:24} constatam que o auto-reparo em
código só compensa quando o crítico é mais capaz que o gerador. Nosso desenho
toma a variável que essa literatura identifica como determinante — a qualidade do
sinal externo — e a maximiza. A ideia de guiar síntese por contraexemplos é
anterior aos modelos de linguagem \citep{solarlezama:06}; nossa condição
\texttt{critic\_cegis} a transpõe para linguagem natural, permitindo variar a
forma do feedback com o conteúdo informacional constante.

\section{Metodologia}

\subsection{Formulação}

Uma tarefa $t$ é um par $(\mathcal{D}_t, (x^\star_t, y^\star_t))$, em que
$\mathcal{D}_t$ são os pares de demonstração e $(x^\star_t, y^\star_t)$ o par de
teste; grades são matrizes de inteiros em $\{0,\dots,9\}$. Um solucionador recebe
$\mathcal{D}_t$ e $x^\star_t$ e produz um programa $\pi$ em Python. A tarefa é
resolvida se e somente se $\pi(x^\star_t) = y^\star_t$ com igualdade exata da
grade inteira.

O recurso controlado é o \textbf{orçamento de chamadas} $B$: o número máximo de
requisições ao modelo por tarefa, somando todos os papéis. Comparamos políticas
$\mathcal{S}$ (diversificar) e $\mathcal{C}$ (iterar) de gasto de $B$:

\begin{equation}
\Delta = \mathbb{E}_{t}\big[\mathbf{1}[\mathcal{C} \text{ resolve } t]\big]
       - \mathbb{E}_{t}\big[\mathbf{1}[\mathcal{S} \text{ resolve } t]\big]
\;>\; 0 .
\label{eq:delta}
\end{equation}

\noindent Manter $B$ idêntico é o controle experimental central: sem ele, a
condição com mais recursos venceria por dispor de mais recursos.

\begin{figure}[t]
\centering
\begin{tikzpicture}[node distance=4.5mm]
  \node[proc] (task) {tarefa: pares de treino\\ + entrada do teste};
  \node[agente, below=of task] (gen) {\textbf{Gerador} (LLM)\\ propõe regra + programa};
  \node[proc, below=of gen] (exec) {\textbf{Executor} (sandbox)\\ roda nos pares de treino};
  \node[dec, below=of exec] (stop) {todos os pares de treino\\ reproduzidos?};
  \node[agente, below=9mm of stop] (crit) {\textbf{Crítico} (LLM)\\ aponta contradições};
  \node[proc, below=of crit] (guard) {filtro anti-vazamento\\ redige grades e código};
  \node[oraculo, right=7mm of crit] (orac) {oráculo\\ $y^\star_t$};
  \node[proc, right=7mm of stop] (final) {avaliação final\\ $\pi(x^\star_t) \overset{?}{=} y^\star_t$};

  \draw[fl] (task) -- (gen);
  \draw[fl] (gen) -- (exec);
  \draw[fl] (exec) -- (stop);
  \draw[fl] (stop) -- node[rot, pos=0.45] {não} (crit);
  \draw[fl] (crit) -- (guard);
  \draw[flt] (orac) -- (crit);
  \draw[fl] (guard.west) -- ++(-0.75,0) |- (gen.west);
  \draw[fl] (stop) -- node[rot, pos=0.45] {sim} (final);
\end{tikzpicture}
\caption{Arquitetura do solucionador. O Gerador nunca vê $y^\star_t$; o Crítico o
vê (linha tracejada) apenas nas condições que o concedem. A restrição central é
que $y^\star_t$ não decide nada: o critério de parada e a seleção do candidato
final consideram apenas os pares de demonstração, e a avaliação final roda uma
única vez, ao término do laço, exclusivamente para medir.}
\label{fig:pipeline}
\end{figure}

\subsection{Condições Experimentais}

A Figura~\ref{fig:pipeline} mostra o laço comum. \texttt{sampling} aloca o
orçamento em largura: cada uma das $B$ chamadas é uma conversa nova com o mesmo
\emph{prompt} inicial, sem histórico nem crítica, e vence o candidato que
reproduz mais pares de demonstração. \texttt{critic} aloca em profundidade: o
Gerador propõe regra e programa, o Crítico aponta contradições em prosa livre, e
o Gerador revisa com o histórico completo. Cada ciclo custa duas chamadas, de
modo que com $B = \orcamento$ resultam \AcritProg{} programas contra os
\AsampProg{} da amostragem.

A adoção de \emph{best-of-N} como linha de base é deliberada: nada obriga quem
dispõe de $B$ chamadas a gastá-las revisando um único programa em série, e medir
a intervenção contra uma alternativa que aloca pior produziria uma vantagem que
informa mais sobre o comparativo do que sobre o método.

Essa comparação responde a uma questão pragmática, não causal: as condições
diferem em quatro eixos simultâneos — existência de feedback, acesso ao gabarito,
orçamento em série ou paralelo, e temperatura (0,8 em \texttt{sampling}, 0,2 nas
demais; sem essa assimetria as $N$ amostras colapsariam em programas quase
idênticos). Para separar os dois eixos de maior interesse, duas condições
adicionais variam um único eixo em relação a \texttt{critic}
(Figura~\ref{fig:matriz}): \texttt{critic\_no\_oracle} recebe o código, a regra e
o relatório de execução, mas nunca $y^\star_t$; \texttt{critic\_cegis} recebe o
mesmo que \texttt{critic}, inclusive $y^\star_t$, mas responde com um único
contraexemplo e uma classe de correção em vocabulário fechado
(\texttt{MISSING\_CASE}, \texttt{WRONG\_TRANSFORM}, \texttt{WRONG\_GEOMETRY},
\texttt{WRONG\_COLOR\_MAP}, \texttt{WRONG\_SCOPE}, \texttt{OTHER}) em vez de prosa
livre.

\begin{figure}[t]
\centering
\begin{tikzpicture}[node distance=3mm]
  \node[celcheia] (c11) {\texttt{critic}\\[2pt] prosa livre,\\ com oráculo};
  \node[celcheia, right=3mm of c11] (c12) {\texttt{critic\_cegis}\\[2pt] contraexemplo,\\ com oráculo};
  \node[celcheia, below=4mm of c11] (c21) {\texttt{critic\_}\\\texttt{no\_oracle}\\[2pt] prosa livre,\\ sem oráculo};
  \node[celvazia, below=4mm of c12] (c22) {não instanciada\\[2pt] (variaria os\\ dois eixos)};

  \node[eixo, above=1.5mm of c11] {prosa livre};
  \node[eixo, above=1.5mm of c12] {estruturada};
  \node[eixo, left=1.5mm of c11] {com\\ oráculo};
  \node[eixo, left=1.5mm of c21] {sem\\ oráculo};

  \draw[{Latex[length=1.3mm]}-{Latex[length=1.3mm]}, black!55] (c11) -- (c12);
  \draw[{Latex[length=1.3mm]}-{Latex[length=1.3mm]}, black!55] (c11) -- (c21);

  \node[cel, text width=53mm, anchor=north]
       at ([yshift=-4mm]$(c21.south)!0.5!(c22.south)$)
       {\texttt{sampling} — linha de base: sem feedback,\\
        orçamento inteiro em candidatos independentes};
\end{tikzpicture}
\caption{As quatro condições nos dois eixos da decomposição. As setas marcam as
comparações que variam um único eixo: na horizontal, a forma do feedback com o
acesso à informação constante; na vertical, o acesso com a forma constante. A
célula inferior direita não é instanciada porque variaria os dois eixos.}
\label{fig:matriz}
\end{figure}

\subsection{Salvaguardas e Análise}

Conceder o gabarito a um agente cria três riscos, tratados separadamente. O
gabarito \textbf{não seleciona}: o critério de parada é idêntico nas quatro
condições — o candidato é aceito quando reproduz todos os pares de demonstração —
e o par de teste roda uma única vez, ao final, apenas para medir; se o laço
parasse ao acertar o teste, a taxa de acerto viraria um limite superior de
oráculo. O gabarito \textbf{não vaza}: um filtro automático redige, na saída do
Crítico, linhas compostas apenas de inteiros e qualquer código, e cada redação é
contabilizada, de modo que o vazamento vira evento mensurável em vez de depender
da cooperação do modelo. O Crítico \textbf{não resolve}: é reinstanciado a cada
rodada, sem histórico, e portanto não pode construir uma solução ao longo do
diálogo. Gerador e Crítico usam o mesmo modelo, para que uma eventual vantagem
não seja atribuível à capacidade de um segundo modelo.

Todas as condições rodam sobre as mesmas \An{} tarefas sorteadas de \divisao{}
com semente \semente{} e $B = \orcamento$, declaradas antes da execução. Em
desenho pareado, o que importa não é a diferença de acurácia agregada, mas
quantas tarefas cada condição resolveu que a outra não \citep{mcnemar:47}: o
teste de McNemar exato descarta as concordâncias e avalia se as discordâncias se
dividem por acaso, $p = 2P(X \le \min(a,b))$ com
$X \sim \mathrm{Binomial}(a+b, 1/2)$. Usamos a versão exata em vez da aproximação
qui-quadrado, pouco confiável com poucas discordâncias. Os intervalos são de
Wilson \citep{wilson:27} sobre a proporção de discordâncias a favor da
intervenção, convertidos para pontos percentuais de acurácia:

\begin{equation}
\mathrm{IC}_{95} =
\frac{\hat{p} + \frac{z^2}{2n} \pm z\sqrt{\frac{\hat{p}(1-\hat{p})}{n} + \frac{z^2}{4n^2}}}
     {1 + \frac{z^2}{n}} .
\label{eq:wilson}
\end{equation}

\noindent Reportá-los é o que distingue a ausência de efeito detectado da
ausência de capacidade de medição. A família de \numComparacoes{} comparações
(cada condição de crítico contra \texttt{sampling}, e \texttt{critic} contra as
outras duas) foi pré-registrada antes da execução, o que exige a correção de
Bonferroni \citep{dunn:61}: $\alpha = 0{,}05/\numComparacoes = \alfaBonf$ por
teste. O par \texttt{no\_oracle} \emph{vs.} \texttt{cegis} fica de fora por
variar os dois eixos. Tarefas interrompidas por erro de API são excluídas da
comparação pareada.

O modelo é \modelo{} nos dois papéis. A escolha é de poder estatístico: um
desenho pareado só discrimina se existirem tarefas que uma condição resolve e a
outra não, e modelos em qualquer extremo — que resolvem tudo na primeira geração
ou falham em ambas — produzem apenas pares concordantes.

\section{Resultados}

\subsection{Comparação em Escala}

\begin{table}[t]
\centering
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{@{}lccccc@{}}
\toprule
Condição & Res. & Acur. & Treino & Prog. & Tok. \\
\midrule
\texttt{critic}     & \AcritRes/\An  & \textbf{\AcritAcc\%} & \AcritTreino\% & \AcritProg{} & \AcritTok\,k \\
\texttt{sampling}   & \AsampRes/\An  & \AsampAcc\%  & \AsampTreino\%  & \AsampProg{}  & \AsampTok\,k \\
\texttt{cegis}      & \AcegisRes/\An & \AcegisAcc\% & \AcegisTreino\% & \AcegisProg{} & \AcegisTok\,k \\
\texttt{no\_oracle} & \AnoorRes/\An  & \AnoorAcc\%  & \AnoorTreino\%  & \AnoorProg{}  & \AnoorTok\,k \\
\bottomrule
\end{tabular}
\caption{Resultados agregados, ordenados por acurácia. ``Treino'' é a fração de
tarefas em que o candidato final reproduz todos os pares de demonstração — o alvo
que as estratégias de fato otimizam; ``Prog.'' é o número médio de programas por
tarefa e ``Tok.'', o consumo médio de tokens.}
\label{tab:exp2}
\end{table}

\begin{figure}[t]
\centering
\begin{tikzpicture}[x=0.145cm, y=1cm]
  \def\ya{2.5} \def\yb{2.0} \def\yc{1.5} \def\yd{1.0} \def\ye{0.5}
  \foreach \x in {-10,-5,0,5} {
    \draw[gray!30] (\x,0.28) -- (\x,2.78);
    \node[font=\scriptsize, gray, below] at (\x,0.28) {\x};
  }
  \draw[thick, gray!70] (0,0.28) -- (0,2.78);
  \node[font=\scriptsize, gray, below] at (-3,-0.02) {diferença de acurácia (pp)};
  % um \draw por comparação: lê \C*LoN e \C*HiN do painel
  \draw[very thick] (\CumLoN,\ya) -- (\CumHiN,\ya);
  \draw[thick] (\CumLoN,\ya-0.09) -- (\CumLoN,\ya+0.09);
  \draw[thick] (\CumHiN,\ya-0.09) -- (\CumHiN,\ya+0.09);
  \draw[very thick] (\CdoisLoN,\yb) -- (\CdoisHiN,\yb);
  \draw[thick] (\CdoisLoN,\yb-0.09) -- (\CdoisLoN,\yb+0.09);
  \draw[thick] (\CdoisHiN,\yb-0.09) -- (\CdoisHiN,\yb+0.09);
  \draw[very thick] (\CtresLoN,\yc) -- (\CtresHiN,\yc);
  \draw[thick] (\CtresLoN,\yc-0.09) -- (\CtresLoN,\yc+0.09);
  \draw[thick] (\CtresHiN,\yc-0.09) -- (\CtresHiN,\yc+0.09);
  \draw[very thick] (\CquatroLoN,\yd) -- (\CquatroHiN,\yd);
  \draw[thick] (\CquatroLoN,\yd-0.09) -- (\CquatroLoN,\yd+0.09);
  \draw[thick] (\CquatroHiN,\yd-0.09) -- (\CquatroHiN,\yd+0.09);
  \draw[very thick] (\CcincoLoN,\ye) -- (\CcincoHiN,\ye);
  \draw[thick] (\CcincoLoN,\ye-0.09) -- (\CcincoLoN,\ye+0.09);
  \draw[thick] (\CcincoHiN,\ye-0.09) -- (\CcincoHiN,\ye+0.09);
  % fill=white impede que as linhas de grade cruzem o texto
  \node[rot, anchor=west] at (-11,\ya+0.22) {\texttt{samp}--\texttt{critic} ($p=\Cump$)};
  \node[rot, anchor=west] at (-11,\yb+0.22) {\texttt{samp}--\texttt{no\_or} ($p=\Cdoisp$)};
  \node[rot, anchor=west] at (-11,\yc+0.22) {\texttt{samp}--\texttt{cegis} ($p=\Ctresp$)};
  \node[rot, anchor=west] at (-11,\yd+0.22) {\texttt{critic}--\texttt{no\_or} ($p=\Cquatrop$)};
  \node[rot, anchor=west] at (-11,\ye+0.22) {\texttt{critic}--\texttt{cegis} ($p=\Ccincop$)};
\end{tikzpicture}
\caption{Intervalos de Wilson (Equação~\ref{eq:wilson}) de 95\% das
\numComparacoes{} comparações pré-registradas, em pontos percentuais; a linha
vertical marca a ausência de
efeito e todos os intervalos a cruzam. As discordâncias, na ordem de cima para
baixo, são \CumDisc{} (\CumSplit), \CdoisDisc{} (\CdoisSplit), \CtresDisc{}
(\CtresSplit), \CquatroDisc{} (\CquatroSplit) e \CcincoDisc{} (\CcincoSplit).
O que a figura evidencia não é uma ordenação, e sim a largura dos intervalos:
mesmo em \An{} tarefas, a medição não separa as hipóteses.}
\label{fig:forest}
\end{figure}

% >>> VEREDITO (resultado principal)
Com as quatro condições medidas nas mesmas \An{} tarefas
(Tabela~\ref{tab:exp2}), a hipótese da Equação~\ref{eq:delta} não se confirma:
\vereditoDois{} (Figura~\ref{fig:forest}). A comparação
mais próxima é \texttt{critic} \emph{vs.}\ \texttt{critic\_no\_oracle}, com
$p$ = \Cquatrop{} e intervalo de \CquatroLo{} a \CquatroHi~pp, que ainda inclui o
zero; o menor $p$ da família é \pMenor{}. A diferença entre \texttt{critic} e
\texttt{sampling} é de \AdeltaCritSamp~pp.

O mecanismo, no entanto, opera como projetado: o Crítico produz \AcritProg{}
programas por tarefa contra \AsampProg{} da amostragem — \AreducaoProg\% a menos
— atingindo a \emph{mesma} consistência com os pares de demonstração
(\AcritTreino\% nas duas condições). Converte orçamento em revisão com eficiência
real; o que não ocorre é a conversão disso em acurácia no teste. A restrição de
formato de \texttt{critic\_cegis} foi obedecida sem exceção (\CegisConf{} das
críticas com rótulo válido) e o filtro anti-vazamento registrou \Aleaks{} eventos
nas \Acriticas{} críticas emitidas — mas nem o formato nem o oráculo se
converteram em vantagem. Entre as quatro, \texttt{critic\_no\_oracle} é a de
menor acurácia bruta e a única que descreve um sistema executável sem gabarito.

\subsection{A Origem das Vitórias}

\begin{figure}[t]
\centering
\begin{tikzpicture}[x=0.048cm, y=1cm]
  \def\yA{2.4} \def\yB{1.8} \def\yC{1.2} \def\yD{0.6}
  \draw[gray!50] (0,0.25) -- (0,2.75);
  \foreach \x in {0,25,50,75,100} {
    \draw[gray!30] (\x,0.25) -- (\x,2.75);
    \node[font=\scriptsize, gray, below] at (\x,0.25) {\x};
  }
  % segmento claro = 1a geração; escuro = ciclo
  \fill[black!18] (0,\yA-0.16) rectangle (\GsampUm,\yA+0.16);
  \fill[black!55] (\GsampUm,\yA-0.16) rectangle (\GsampTot,\yA+0.16);
  \fill[black!18] (0,\yB-0.16) rectangle (\GcritUm,\yB+0.16);
  \fill[black!55] (\GcritUm,\yB-0.16) rectangle (\GcritTot,\yB+0.16);
  \fill[black!18] (0,\yC-0.16) rectangle (\GcegisUm,\yC+0.16);
  \fill[black!55] (\GcegisUm,\yC-0.16) rectangle (\GcegisTot,\yC+0.16);
  \fill[black!18] (0,\yD-0.16) rectangle (\GnoorUm,\yD+0.16);
  \fill[black!55] (\GnoorUm,\yD-0.16) rectangle (\GnoorTot,\yD+0.16);
  \node[font=\scriptsize, anchor=east] at (-3,\yA) {\texttt{sampling}};
  \node[font=\scriptsize, anchor=east] at (-3,\yB) {\texttt{critic}};
  \node[font=\scriptsize, anchor=east] at (-3,\yC) {\texttt{critic\_cegis}};
  \node[font=\scriptsize, anchor=east] at (-3,\yD) {\texttt{critic\_no\_oracle}};
  \node[font=\scriptsize, anchor=west] at (\GsampTot+3,\yA)  {\GsampTot{} \ (\GsampCiclo)};
  \node[font=\scriptsize, anchor=west] at (\GcritTot+3,\yB)  {\GcritTot{} \ (\GcritCiclo)};
  \node[font=\scriptsize, anchor=west] at (\GcegisTot+3,\yC) {\GcegisTot{} \ (\GcegisCiclo)};
  \node[font=\scriptsize, anchor=west] at (\GnoorTot+3,\yD)  {\GnoorTot{} \ (\GnoorCiclo)};
\end{tikzpicture}
\caption{Decomposição das vitórias, em tarefas resolvidas de \An{}. O segmento
claro são as decididas na primeira geração, comum às quatro condições; o escuro,
as atribuíveis ao ciclo. Os rótulos dão o total e, entre parênteses, a parcela do
ciclo. A ordenação pelo total (\GcritTot{}, \GsampTot{}, \GcegisTot{},
\GnoorTot{}) \emph{inverte} quando se olha apenas o ciclo (\GcegisCiclo{},
\GcritCiclo{}, \GnoorCiclo{}, \GsampCiclo{}): as três condições de crítico passam
à frente da linha de base.}
\label{fig:decomposicao}
\end{figure}

A primeira chamada é idêntica nas quatro condições — mesmo \emph{prompt},
histórico vazio, nenhum crítico envolvido —, diferindo apenas na temperatura.
Vitórias decididas ali não distinguem estratégia alguma.

% >>> VEREDITO (origem das vitórias)
A Figura~\ref{fig:decomposicao} mostra que \vereditoTres{}. Restringindo cada par
às tarefas que a primeira geração não resolveu em nenhuma das duas condições, a
medição discrimina em cerca de um quinto da amostra e todos os
\numComparacoes{} $p$-valores ficam entre \RpMenor{} e \RpMaior{}.

Duas leituras decorrem daí. A primeira é metodológica: o poder efetivo de uma
comparação entre arquiteturas de agentes é muito menor do que $n$ sugere, porque
boa parte da amostra é decidida antes de qualquer estratégia agir. A segunda vai
na direção oposta ao número bruto: descontada a primeira geração, as três
condições de crítico ficam \emph{acima} de \texttt{sampling} em vitórias do ciclo
(\GcegisCiclo{}, \GcritCiclo{} e \GnoorCiclo{} contra \GsampCiclo{}). As
diferenças são pequenas e nenhuma é significativa, mas a direção depende de qual
quantidade se observa — dependência invisível em qualquer relato que reporte
apenas acurácia agregada.

O efeito replica numa medição independente sobre as mesmas tarefas, com apenas
\texttt{sampling} e \texttt{critic} e um protocolo de crítica anterior: das
\BsampRes{} vitórias de \texttt{sampling}, \BsampPrimeira{} vêm da primeira
geração, e das \BcritRes{} de \texttt{critic}, \BcritPrimeira{}, deixando o placar
do ciclo em \BsampCiclo{}\,$\times$\,\BcritCiclo{}, exatamente empatado; nas
\BdiscrimN{} tarefas restantes a comparação também não distingue as condições
(\Bdelta~pp, $p$ = \Bp{}, intervalo de \BicBaixo{} a \BicAlto~pp). Sob orçamento
igualado, portanto, \vereditoUm{}, com qualquer vantagem limitada a
\BicAltoAbs~pp.

O confundimento é eliminável por construção: basta que a primeira condição a
alcançar uma tarefa grave a primeira geração e as demais a repitam, com a
repetição ainda debitada do orçamento. Além de removê-lo, isso economiza
$(\text{condições} - 1) \times \text{tarefas}$ chamadas por bloco,
\SeedEconomia{} num bloco de quatro condições. Nenhuma das medições aqui
reportadas o utiliza.

\subsection{Reprodutibilidade da Ordenação}

\begin{figure}[t]
\centering
\begin{tikzpicture}[x=0.0135cm, y=0.032cm]
  \draw[gray!50] (0,0) -- (0,100);
  \foreach \y in {0,25,50,75,100} {
    \draw[gray!25] (0,\y) -- (285,\y);
    \node[font=\scriptsize, gray, anchor=east] at (-6,\y) {\y\%};
  }
  \foreach \x in {30,60,90,135,180,270} {
    \node[font=\scriptsize, gray, anchor=north] at (\x,-3) {\x};
  }
  \node[font=\scriptsize, gray, anchor=north] at (150,-22) {tarefas na subamostra};
  \draw[very thick] (30,\SubDirAN) -- (60,\SubDirBN) -- (90,\SubDirCN)
                 -- (135,\SubDirDN) -- (180,\SubDirEN) -- (270,100);
  \draw[thick, dashed, gray!70] (30,\SubOrdAN) -- (60,\SubOrdBN) -- (90,\SubOrdCN)
                 -- (135,\SubOrdDN) -- (180,\SubOrdEN) -- (270,100);
  \draw[gray!60] (0,50) -- (285,50);
  \node[rot, anchor=west] at (95,88) {direção \texttt{critic} $>$ \texttt{samp}};
  \node[rot, anchor=west] at (95,22) {ordenação das quatro};
  \node[rot, anchor=east] at (128,50) {acaso};
\end{tikzpicture}
\caption{Reprodutibilidade da ordenação sob subamostragem das próprias \An{}
tarefas (\SubRep{} repetições por tamanho, sem reposição, preservando o
pareamento). A linha cheia é a fração de subamostras em que \texttt{critic}
supera \texttt{sampling}; a tracejada, a fração em que a ordenação completa das
quatro coincide com a observada em \An{}. Ambas convergem a 100\% em \An{} por
construção.}
\label{fig:estabilidade}
\end{figure}

As comparações acima não são significativas, mas a ordenação que produzem é
frequentemente o que sobra de um estudo desses. Para medir quanto dela é
reprodutível, subamostramos o conjunto fechado de \An{} tarefas sem reposição,
preservando o pareamento, em \SubRep{} repetições por tamanho
(Figura~\ref{fig:estabilidade}).

Com \SubNa{} tarefas, a direção observada em \An{} se reproduz em \SubDirA\% das
subamostras — indistinguível de um sorteio — e a banda de 5 a 95\% da diferença
de acurácia vai de \SubBandaAlo{} a \SubBandaAhi~pp. Com \SubNb{} tarefas, ainda
\SubDirB\%. A ordenação completa das quatro condições é substancialmente mais
frágil: \SubOrdA\% em \SubNa{} tarefas e \SubOrdB\% em \SubNb{}. Estudos desta
classe conduzidos em algumas dezenas de tarefas não estão medindo direção, e a
ordenação entre mais de duas condições é essencialmente incidental. Note-se que
a subamostragem mede apenas variância de \emph{amostra}: uma reexecução real
acrescenta a variância do modelo, de modo que esses valores são um piso otimista.

O mesmo conjunto quantifica o risco de extrapolar antes do fechamento. Sobre as
primeiras \ProjN{} tarefas, extrapolar linearmente as proporções da comparação
mais próxima produzia $p$ = \ProjPextrapolado{}, valor que teria atravessado a
correção de Bonferroni; o conjunto fechado deu $p$ = \ProjPfinal{}. É esse o
mecanismo que produz literaturas contraditórias sem exigir má conduta: um estudo
que parasse antes do fechamento reportaria uma direção diferente, com $p$ não
significativo, reportável como observação direcional, e ruído \citep{button:13}.

\section{Discussão e Limitações}

A leitura defensável é pragmática: sob orçamento igualado, alocar em diversidade
não é distinguível de alocar em revisão dirigida — nem mesmo quando a revisão é
guiada por um oráculo indisponível a qualquer sistema autônomo. Como o desenho
favorece a via iterativa por construção, isso indica que a informação
privilegiada, isoladamente, não é o fator limitante; não sustenta, porém, que a
assimetria de informação seja irrelevante, já que os intervalos admitem efeitos
que a medição não detectaria.

O achado mais consequente é sobre \emph{quanto de uma comparação entre
arquiteturas de agentes é atribuível à arquitetura}. O confundimento da primeira
geração não é específico deste arranjo: qualquer comparação entre esquemas que
partam do mesmo \emph{prompt} o herda, e ele não aparece em métrica agregada
alguma.

Uma implicação prática: contabilizamos orçamento em chamadas, mas contado em
tokens o quadro se deteriora para as condições de crítico, que consomem de
\AtokExcessoMin\% a \AtokExcessoMax\% mais tokens por tarefa, porque o histórico
acumulado e o gabarito trafegam a cada crítica. Sob orçamento em tokens — a
unidade de custo efetiva — o sinal poderia inverter.

\paragraph{Limitações.} (i) Metade do resultado vem de uma chamada comum às
condições, com correção conhecida e não aplicada. (ii) \texttt{critic} e
\texttt{critic\_cegis} consultam o gabarito durante o laço e medem a validação por
oposição como limite superior, não um método implantável; apenas
\texttt{critic\_no\_oracle} descreve um sistema executável. (iii) A linha de base
foi coletada em data anterior às condições de crítico, e o modelo é hospedado:
as comparações com \texttt{sampling} carregam um confundimento temporal que as
comparações entre críticos não têm. (iv) Uma execução por tarefa — o pareamento
mitiga o ruído do modelo, não o elimina. (v) A acurácia absoluta e a própria
existência de pares discordantes dependem do modelo, de modo que as conclusões se
referem ao regime de capacidade de \modelo{}.

\section{Conclusão}

% >>> VEREDITO (conclusão)
Sob orçamento fixo de chamadas para síntese de programas no ARC-AGI-1, quatro
condições que variam isoladamente o acesso ao oráculo e a forma do feedback não
se separam em \An{} tarefas: \vereditoDois{}. O crítico atinge a mesma
consistência com os pares de demonstração usando \AreducaoProg\% menos programas,
sem que isso se converta em acerto no teste.

O resultado mais transferível independe de qual condição prevaleça:
\vereditoTres{}. Descontada essa parcela, a medição discrimina em cerca de um
quinto da amostra e a ordenação inverte em relação ao número bruto — ordenação
que, ademais, é pouco reprodutível: em \SubNb{} tarefas ela se repete em apenas
\SubOrdB\% das subamostras do mesmo conjunto. O passo seguinte não é aumentar
$n$, e sim eliminar o confundimento da primeira geração e medir o orçamento em
tokens.

\bibliography{artigo}

\end{document}
